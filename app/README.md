# Accor Hotels API boilerplate

## First-time process

1. Install the dependencies:
  ```bash
  $ npm install
  ```

2. Configure your database connection in `./client.js`.

## Start the Server

```bash
$ npm start
```
